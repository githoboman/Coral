[package]
name = "corral-core"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]

[features]
# WASM bindings for the frontend (C-107). Gated so native builds stay lean.
wasm = []

[lints]
workspace = true
