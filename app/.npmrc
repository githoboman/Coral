# The Coral lineage's lockfiles assume --legacy-peer-deps (see CLAUDE.md §6).
# Vercel/CI run a bare `npm install`, so pin the flag here rather than relying on
# a per-command switch. Fixes ERESOLVE on the vitest / @vitest/coverage-v8 peer.
legacy-peer-deps=true
