// types/ika.d.ts
//
// Do NOT redeclare @ika.xyz/sdk here — let the real package types from
// node_modules take effect. This file is intentionally empty so TypeScript
// uses the SDK's own declarations.
//
// If you need to augment types, do so with `declare module` and only add
// fields that are genuinely missing from the SDK's published types.
